<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/toChat"
        android:title="Chat Page"
        android:orderInCategory="10"
        app:showAsAction="always"/>  <!-- This uses showAsAction=always, so look at icon= parameter -->

    <item android:id="@+id/toWeather"
        android:orderInCategory="20"
        app:showAsAction="always"
        android:title="Weather Forecast"/>    <!-- This uses showAsAction=always, so look at icon= parameter -->


    <item android:id="@+id/toLogin"
        android:title="Go back to Login"
        app:showAsAction="always"
        android:orderInCategory="30"/> <!-- This uses showAsAction=never, so look at title= parameter -->


</menu>
